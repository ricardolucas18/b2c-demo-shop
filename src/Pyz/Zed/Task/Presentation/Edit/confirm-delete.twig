{% extends '@Gui/Layout/layout.twig' %}

{% set widget_title = 'Warning' %}

{% block action %}
    {{ backActionButton('/task', 'Tasks List' | trans) }}
{% endblock %}

{% block content %}

    {% embed '@Gui/Partials/widget.twig' %}

        {% block widget_content %}
            <div class="row">
                <div class="col-sm-12">
                    <p>{{ "You are trying to delete task" | trans }}: <b>{{ task.idTask }}</b></p>
                    <p>{{ "It will be deleted from everywhere" | trans }}</p>
                </div>
            </div>
            <hr/>
            <div class="row">
                <div class="col-sm-12">
                    <a href="{{ app.request.headers.get('referer') }}" class="btn btn-back">{{ 'Back' | trans }}</a>
                    {{ form_start(taskDeleteConfirmForm) }}
                        <input type="hidden" name="id-task" value="{{ task.idTask }}" />
                        <input type="submit" class="btn btn-danger safe-submit" value="{{ 'Delete' | trans }}" />
                    {{ form_end(taskDeleteConfirmForm) }}
                </div>
            </div>
        {% endblock %}

    {% endembed %}

{% endblock %}
